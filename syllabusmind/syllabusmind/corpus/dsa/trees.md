# Trees & BSTs

Study notes for the demo (replace or extend with real course material).

Inorder traversal of a binary search tree visits keys in ascending order.

A balanced binary search tree has height about log n, so search is O(log n).

An unbalanced BST built from sorted input degenerates into a list, so search is O(n).
