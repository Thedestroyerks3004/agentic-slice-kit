# Sorting algorithms

Study notes for the demo (replace or extend with real course material).

Merge sort takes O(n log n) in every case.

A stable sort keeps equal keys in their original order, and merge sort is stable.

Quicksort degrades to O(n squared) when pivots split the input badly, for example a sorted array with a first-element pivot.
