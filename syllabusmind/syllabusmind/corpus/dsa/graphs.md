# Graph traversal

Study notes for the demo (replace or extend with real course material).

Breadth-first search uses a queue and depth-first search uses a stack.

Breadth-first search finds shortest paths by edge count in an unweighted graph, and depth-first search does not.

Breadth-first search on an adjacency list takes O(V plus E) time.
