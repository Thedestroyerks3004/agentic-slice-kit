# Hash tables

Study notes for the demo (replace or extend with real course material).

A hash table lookup is O(1) on average and O(n) when all keys collide.

A collision is two different keys mapping to the same slot.

A hash table does not keep keys in sorted order.
